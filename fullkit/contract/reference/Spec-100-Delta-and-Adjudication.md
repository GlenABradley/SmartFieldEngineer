# Slice 1 Spec 1.00 — delta and adjudication

September 18, 2026 · GPT Sol response after Grok's first word on Claude's adversarial pass

**Keep two business Homes.** Slice 1 is one trusted owner keeping two books on one laptop. Wrong-context closeouts are a real first-release failure. That requirement is distinct from sponsor IAM or hostile local-user defense. No hub, voice, robot, or whole-product scope is pulled into Slice 1.

| Finding | Sol decision | Effective patch |
|---|---|---|
| Time/parts reservation ambiguity | Accept Grok | Method and command #16 are envelope.put; ID envelope_id; view envelopes. No old-name alias. STALE_RESERVATION remains the existing closed compatibility code for this time head only. |
| Magic sourcing prefix | Accept; make attribution honest | `stabilization_done && (valid_evidence || valid_source_reference)`. No prefix gate. Operator attestation stays claimed, not independently measured safety. |
| WAL backup | Accept; choose implementation | SQLite Backup API is default, with writer queue paused and retention barrier. Standalone snapshot, snapshot-derived blob list, verified manifest. No hot main-file/sidecar guess. VACUUM INTO needs qualification if substituted. |
| Import repetition / partial failure | Accept; fill changed-corpus seam | Canonical source digest, batch/report receipt, stable legacy map, final atomic fact/map/report transaction. Same corpus returns IMPORT_ALREADY_APPLIED without new facts. Changed corpus reuses unchanged mappings and exposes changed-source conflicts instead of double-counting. |
| NTFS aliases | Accept bounded rigor | OS exclusive handle remains ownership; volume/file identity and final path establish the opened object/root. Junction and subst tests. No PID-file takeover. |
| Encryption observation | Accept | Best-effort without extra elevation; denied/unavailable is unknown. Capture continues. Commissioning source/time distinguished from live observation. |
| In-process versus shipped boundary | Accept Grok, qualify the security language | Shipped QProcess; in-process only labeled dev/test/spike. Transport declared. Context tests pass in both; process separation is not tenant authorization or hostile-admin defense. |
| Two Homes allegedly sponsor scope | Reject dropping Home B; accept explicit scope label | One owner, two books; no second-human/admin/network-tenant threat claim. Context fence belongs inside 152–240 hours. |

Three additional consistency corrections are necessary for those fills to work:

1. The intent journal retains semantic_request as well as its fingerprint. A hash cannot reconstruct a command that never reached the core.
2. Phase fingerprint covers the whole phase payload, including digest expectation. Foreign-context denial cannot consume another Home's token.
3. Restore preserves source business/evidence content; its own documented audit/receipt may append to the source ledger. A literal whole-DB unchanged-hash test would contradict durable command receipts.

The new spec is standalone: the mandatory suite is embedded instead of referring Claude to an earlier Sol merge. The pre-answered attack table now states required tests rather than claiming review is preemptively defeated. A real finding remains welcome, including inside a supposedly covered case.

Only new domain/result identifier: **IMPORT_ALREADY_APPLIED**, a maintenance success discriminator. Standard numeric JSON-RPC protocol errors are distinguished from that closed domain table. No new discretionary application error names.

**Cost:** retain provisional 152–240 hours; explicitly absorb 4–8 hours for these backup/import/file-identity primitives inside existing storage/backup bands. Revise if actual work exceeds the bands. No hardware or subscription change. Application 0.2.0 and Spec 0.99 are unchanged.

**Handoff:** give Claude Full-Kit-Slice-1-Build-Spec-100.md alone, plus an actual 0.2.0 import kit only if supplied. This delta is for Glen/Grok; it is not required by the standalone specification. Effective-profile machine schemas and implementation tests remain to be generated; no new running application or Windows qualification is claimed here.
